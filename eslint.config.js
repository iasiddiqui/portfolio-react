export default [
  {
    // Other ESLint configurations here...
    rules: {
      // Add this rule to suppress warnings
      'no-unused-vars': 'warn', // Or 'off' if you don't want the warning at all
    },
  },
];
